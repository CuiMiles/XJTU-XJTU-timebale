<view class="page"
  ><view class="title">我的课程</view
  ><view class="muted">本机存储 · 无账号 · 无云同步</view
  ><view wx:if="{{broken}}" class="notice"
    >数据读取异常，未覆盖原始数据。请先复制原始数据留存，再恢复有效备份。<button
      bindtap="raw"
    >
      复制原始数据
    </button></view
  ><view class="panel"
    ><button class="primary" bindtap="add">＋ 手动添加课程</button
    ><view class="row"
      ><button style="flex: 1" data-mode="import" bindtap="transfer">
        导入课表</button
      ><button style="flex: 1" data-mode="backup" bindtap="transfer">
        备份与恢复
      </button></view
    ></view
  ><view wx:if="{{!courses.length ? !broken : false}}" class="empty muted"
    >还没有课程，从第一门开始吧。</view
  ><view wx:for="{{courses}}" wx:key="id" class="panel"
    ><view style="font-size: 31rpx; font-weight: 600">{{item.name}}</view
    ><view class="muted"
      >周{{item.day}} · 第{{item.sectionText}}节 · {{item.room ||
      '教室待定'}}</view
    ><view class="muted">第 {{item.weekText}} 周</view
    ><picker
      range="{{item.weekChoices}}"
      data-index="{{index}}"
      bindchange="viewOnce"
      ><view class="picker link">查看某一次 / 调整停课日课程 ▾</view></picker
    ><view class="row"
      ><button style="flex: 1" data-id="{{item.id}}" bindtap="edit">编辑</button
      ><button
        style="flex: 1"
        class="danger"
        data-id="{{item.id}}"
        bindtap="remove"
      >
        删除
      </button></view
    ></view
  ><view wx:if="{{adjustments.length}}" class="panel"
    ><view class="title" style="font-size: 32rpx">单次调整记录</view
    ><view class="muted">取消的课程也可在这里恢复。</view
    ><view wx:for="{{adjustments}}" wx:key="key" class="line"
      ><view>{{item.name}}</view
      ><view class="muted"
        >原日期 {{item.originalDate}}<br />{{item.cancelled ? '本次已取消' :
        '调整至 ' + item.date + ' · 第' + item.sections + '节 · ' +
        item.room}}</view
      ><button data-index="{{index}}" bindtap="restore">
        恢复原安排
      </button></view
    ></view
  ><button class="danger" bindtap="clearAll">清空所有数据</button
  ><view class="muted" style="text-align: center"
    >小交课表 V1 · 学生个人工具<br />非西安交通大学官方产品<br />卸载小程序或清理微信数据可能丢失课表，请定期备份。</view
  ></view
>
